# my-blog Agent 写作指南

本指南适用于仓库中的博客文章、项目说明和其他面向读者的文档。除非用户明确提出不同要求，新增或改写内容时遵循以下规则。

## 核心原则

1. 文档必须独立成篇，不依赖读者了解讨论过程、修改记录、提交历史或其他未提供的上下文。
2. 先让读者看懂问题，再解释处理流程，最后才介绍技术名称。
3. 使用真实代码和项目文档核对事实，但不要把文章写成源码注释或文件清单。
4. 不夸大 Agent 或自动化能力，明确区分已经实现的功能、设计目标、尚未完成的能力和作者提出的改进方向。
5. 文章的目标是帮助读者理解系统为什么这样设计，而不只是记住模块名称。

## 目标读者与语气

- 默认面向具备基本编程概念、但不了解当前项目和相关架构的技术初学者。
- 使用平静、清楚、有耐心的语气，像经验丰富的开发者向新人解释系统。
- 不写成产品宣传、学术论文或营销文案。
- 不卖弄复杂性，不使用“革命性”“自主进化”“智能涌现”等夸张说法。
- 可以指出不足，但应说明它造成的实际影响和合理的改进方向，避免为了批评而批评。

## 标题与开头

- 标题应直接说明文章要解释的问题或读者最终会弄懂什么。
- 可以使用适度的悬念或比喻，但不能含糊、夸张或误导。
- 推荐标题形式：
  - `一个“会学习”的新闻爬虫是怎样工作的`
  - `网站改版后，新闻爬虫如何发现问题`
  - `为什么 Agent 不应该处理每一次请求`
- 开头按照以下顺序展开：
  1. 从读者熟悉的实际场景切入。
  2. 说明场景背后的工程困难。
  3. 引出项目采用的解决方法。
- 不要在开头罗列技术栈、模块名、目录结构或缩写。

## 文章结构

- 使用问题式二级标题，让读者只看目录也能理解文章路线。
- 一个章节集中回答一个问题，不要在同一章节中来回切换多个主题。
- 长篇工程解释默认采用以下叙事顺序：

```text
现实问题
→ 核心概念
→ 正常处理流程
→ 陌生输入或复杂情况
→ 验证方法
→ 失败与安全边界
→ 长期运行方式
→ 可以迁移的工程原则
```

- 先介绍常见、正常、容易理解的路径，再讨论 Agent、异常、安全和未来改进。
- 根据主题复杂度决定篇幅。深度工程文章通常控制在 6,000–9,000 个中文字符，但不能为了达到长度重复表达或堆砌背景。

## 术语使用

- 首次出现专业名词时，先说明中文含义，再给出英文名称。
- 后文优先使用中文称呼，不要反复堆叠英文名。
- 缩写首次出现时必须解释它是什么以及为什么与当前问题有关。
- 避免连续使用没有解释的框架名、模块名和缩写。
- 尽量不用以下表达，除非确实必要并已经解释：
  - 控制面、数据面、热路径
  - schema gate、quality guard
  - 能力编排、认知闭环、智能涌现
- 不要写成：

  > Discoverer 通过 Queue 进入 Runner，再由 Registry Matcher 完成路由。

- 应写成：

  > 系统先根据网址找到对应的查找脚本，再把任务放入后台队列等待执行。

## 解释复杂概念

每个复杂概念尽量按照以下顺序说明：

1. 它要解决什么实际问题。
2. 用生活化比喻建立直觉。
3. 说明项目中的真实实现。
4. 说明它不能解决什么或当前还存在哪些边界。

比喻必须服务于准确理解，不能代替技术解释。例如，可以把独立工作目录比喻成“草稿本”，随后仍要解释它如何限制文件修改，以及它为什么不能代替代码执行沙箱。

## 示例与代码

- 尽量使用一个具体的虚构场景贯穿全文，不要每节更换互不相关的例子。
- 示例网址使用 `example.com`、`example.org` 或其他保留域名，避免无意中指向真实页面。
- 示例应短小、具体，并直接服务于当前概念。
- 适合使用：
  - 简短网址和输入输出示例
  - 文本流程图
  - 编号步骤
  - 少量伪代码或经过简化的规则
- 避免使用：
  - 大段源码
  - 没有解释的配置文件
  - 只为展示技术复杂度而加入的代码
  - 与正文没有直接关系的架构图
- 引用真实代码时，只保留理解当前结论所需的最小片段，并用普通语言解释。

## 技术准确性

- 写作前阅读相关 README、设计文档、核心实现和测试，不能只根据文件名或项目简介推断行为。
- 当设计文档与代码不一致时，以当前代码实际行为为准，并在必要时说明差异。
- 不要把预留接口、占位程序或未来计划写成已经投入运行的功能。
- 涉及数量、版本或运行状态时注明统计时间，避免把动态信息写成永久事实。
- 对安全能力使用准确范围。例如，“独立工作目录限制文件修改”不等于“自动生成代码已在强沙箱中运行”。
- 对无法从代码确认的结论使用保守表达，不要为了让叙事完整而编造实现细节。

## 段落与排版

- 段落保持简短，一般为两到四句话。
- 一个段落只表达一个主要意思。
- 二级标题负责推进问题；只有确实存在清晰子问题时才增加三级标题。
- 编号列表用于有先后顺序的流程，无序列表用于并列信息。
- 粗体只强调关键结论，不要每段都使用。
- 代码块只展示网址、状态、短流程或必要代码。
- 长文结尾应提炼少量可迁移原则，不要逐段重复全文。
- Markdown 标题与正文、列表与前文之间保留空行，保证渲染正确。

## 推荐写作流程

1. 明确文章要回答的核心问题和目标读者。
2. 阅读项目文档与代码，整理已经确认的事实、仍有疑问的内容和当前实现边界。
3. 选择一个能够贯穿全文的具体示例。
4. 先写问题式大纲，再按照“问题—直觉—实现—边界”补充各节。
5. 完成后删除重复背景、无作用术语和与主线无关的代码。
6. 检查读者是否可以在不了解项目历史的情况下独立理解全文。
7. 运行博客内容检查与静态构建，确认标题、摘要、目录、列表页和 RSS 正常生成。

## 发布前检查

- 标题是否直接说明文章主题？
- 开头是否先讲现实问题，而不是先讲技术栈？
- 每个必要术语是否在首次出现时得到解释？
- 是否存在可以改成普通中文的英文术语？
- 示例是否贯穿主线并帮助理解？
- 事实是否与当前代码一致？
- 是否清楚区分已实现能力和未来改进？
- 是否诚实说明关键限制而没有过度展开？
- 文章是否可以脱离讨论和变更历史独立阅读？
- 是否通过内容检查和静态构建？

一句话概括本博客的默认风格：**先让读者看懂问题，再理解流程，最后才接触技术名称。**
